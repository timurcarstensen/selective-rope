[build-system]
requires = ["uv_build>=0.10.2,<0.11.0"]
build-backend = "uv_build"

[project]
name = "copying"
version = "0.1.0"
description = "Copying synthetic task for Selective RoPE"
requires-python = ">=3.12"
dependencies = []

[tool.uv.build-backend]
module-root = "src"
